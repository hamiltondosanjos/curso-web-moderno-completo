
//Se nota for >= 7, então retorna 'Aprovado', senão retorna ''Reprovado
const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'

console.log(resultado(7.1))

//ou: const status = nota >=7 ? 'Aprovado' : 'Reprovado'